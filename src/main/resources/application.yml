
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sa_characters_db
    username: postgres
    password: cristiano1234
    driver-class-name: org.postgresql.Driver
  servlet:
    multipart:
      max-file-size: 4MB
      max-request-size: 4MB

  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    flyway:
      locations: classpath:db/migration
      baseline-on-migrate: true